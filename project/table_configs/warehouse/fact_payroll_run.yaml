table_name: fact_payroll_run
layer: warehouse
table_type: fact
source_table: ${ref('stg_payroll_runs')}
columns:
  - payroll_id
  - employee_id
  - pay_period_id
  - pay_date
  - gross_pay
  - net_pay
  - tax_withheld
  - deductions
  - reimbursements
  - hours_worked
  - overtime_hours
  - status
  - created_at
  - updated_at
partition_by: pay_date
cluster_by:
  - employee_id
  - pay_date
  - status
primary_keys:
  - payroll_id
description: "Payroll run fact table - one row per payroll transaction"
tags:
  - fact
  - payroll
  - finance
incremental: true
dataset_id: payroll_warehouse
dependencies:
  - stg_payroll_runs

